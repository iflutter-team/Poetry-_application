import 'package:flutter/material.dart';

Widget commonIcon({IconData? icon, Color? color, double? size, double? fill}) {
  return Icon(
    icon,
    color: color,
    size: size,
    fill: fill,
  );
}
